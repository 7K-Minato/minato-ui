apiVersion: v1
kind: Service
metadata:
  name: {{ include "minato-ui.fullname" . }}
  labels:
    {{- include "minato-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minato-ui.selectorLabels" . | nindent 4 }}
